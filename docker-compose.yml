version: "3"

services:
  db:
    image: postgres:latest
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_PASSWORD=123456
      - POSTGRES_DB=test

  web:
    build: .
    ports:
      - "80:8080"
    depends_on:
      - db
